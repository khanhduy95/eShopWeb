class Configuration {
    localhost = "https://localhost:44320";
    CATALOG_ITEM = this.localhost + "/api/CatalogItems";
    CATALOG_TYPE = this.localhost + "/api/CatalogType";
    
}
export default Configuration;